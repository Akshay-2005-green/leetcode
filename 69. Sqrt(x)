class Solution(object):
    def mySqrt(self, x):
        st=0
        end=x
        while st<=end:
            mid=(st+end)//2
            if mid*mid==x:
                return mid
            
            elif mid*mid>x:
                end=mid-1
            else:
                 st=mid+1
        return end

        
